import logo from '.logo.svg';
import '.App.scss';

import { useState } from 'react';
import { Route, Routes, useLocation } from 'react-router-dom';

import Main from '.componentsMainMain.js';
import Quiz from '.componentsQuizQuiz.js';
import Feedback from '.componentsFeedbackFeedback.js';
import Result from '.componentsResultResult.js'

function App() {
    const [location, setLocation] = useState(useLocation());
    const [answer, setAnswer] = useState({
        one 0,
        two 0,
        three 0
    });

    const onClickAnswer = (path) = {

    }
    


    return (
        div className=App
            Routes 
                Route path='' element={Main path='quiz1' } 
                Route path='quiz1' element={Quiz currentPath='quiz1' nextPath='quiz2' answer={answer}} 
                Route path='quiz2' element={Quiz currentPath='quiz2' nextPath='quiz3' answer={answer}} 
                Route path='quiz3' element={Quiz currentPath='quiz3' nextPath='feedback' answer={answer}} 
                Route path='feedback' element={Feedback path='result' } 
                Route path='result' element={Result}
            Routes
        div
    );
}

export default App;
